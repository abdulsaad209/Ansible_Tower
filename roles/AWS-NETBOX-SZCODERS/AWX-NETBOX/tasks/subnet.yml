---
- name: Get subnet info from AWS for {{ account.name }}
  amazon.aws.ec2_vpc_subnet_info:
    region: "{{ item }}"
    profile: "{{ aws_profile }}"
  loop: "{{ aws_region }}"
  register: aws_subnet_info