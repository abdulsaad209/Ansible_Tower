---
# Zabbix API connection info
#zabbix_api_url: "http://192.168.12.136/zabbix/api_jsonrpc.php"
zabbix_api_user: "ansible-api"
zabbix_api_pass: "Ansible@1234"
zabbix_url: "http://192.168.12.136/zabbix/"
zabbix_host_description: "test"
zabbix_host_status: "enabled"
zabbix_host_groups: '["szcoders-corp","Linux servers"]'
zabbix_agent_server: "10.0.0.36"  # zabbix server IP address
zabbix_api_use: true # tells to add a host through an API
zabbix_interface_port: "10050" # agent port
zabbix_link_templates: ["Linux by Zabbix agent"] # a list of used templates
zabbix_interface_type: agent # tells to use zabbix agent
zabbix_interface_use_ip: "0" # defines to use ip - 0 or dns - 1 to connect to host
zabbix_interface_ip: "" 


# Host defaults (can be overridden in playbook)
#zabbix_host_name: "{{ inventory_hostname }}"
#zabbix_host_ip: "{{ ansible_host | default(inventory_hostname) }}"
#zabbix_host_groups:
 # - "Linux servers"
#zabbix_host_templates:
#  - "Template OS Linux"
#zabbix_host_interfaces:
#  - type: 1          # 1=agent
#    main: 1
#    useip: 1
#    ip: "{{ ansible_host | default(inventory_hostname) }}"
#    dns: ""
#    port: 10050


